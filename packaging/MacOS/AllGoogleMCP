#!/bin/bash
# Launcher: opens the All Google MCP status panel (native window).
set -euo pipefail
HERE="$(cd "$(dirname "$0")" && pwd)"
# shellcheck source=lib.sh
source "$HERE/lib.sh"
run_all_google_mcp setup
